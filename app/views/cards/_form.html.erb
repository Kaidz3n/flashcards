<%= simple_form_for @card do |f| %>
	<%= f.input :original_text, :hint => "Type english word" %>
	<%= f.input :translated_text, :hint => "Введите перевод слова кириллицей" %>
	<%= f.input :review_date %>
	<%= f.button :submit, 'Создать карточку' %>
<% end %>